# 更新日志

本文件记录插件的重要变更，格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [2.11.0] - 2026-09-24

### 变更

- **回复巡检默认只在中晚两个时段、每 30 分钟一轮**：`interact_reply_cron` 默认值由
  `*/5 8-23 * * *` 改为 `0,30 12-13,20-23 * * *`（等价写法 `*/30 12-13,20-23 * * *`），
  `interact_reply_jitter` 由 `60` 改为 `120`。默认只在**中午 12:00–14:00** 与
  **晚上 20:00–24:00** 巡检，实际触发点为 12:00 / 12:30 / 13:00 / 13:30 与
  20:00 / 20:30 / 21:00 / 21:30 / 22:00 / 22:30 / 23:00 / 23:30（合计 12 轮/天，
  各加 0~2 分钟抖动）；最后一轮 23:30，此后到次日 12:00 之间不巡检，
  **时段外的评论要等到下一个时段才会被回复**（例如 23:40 的评论，次日中午 12:00 才处理）。
  这样每轮只在自己活跃的时段发请求，比全天每 5 分钟一轮（约每天 300~600 次）
  更不容易触发风控与登录态失效；时段与间隔都可以在面板里调整。
  `/空间回复` 会写明「每 30 分钟一轮｜时段：每天 12:00-14:00、20:00-24:00｜下一轮 20:00｜
  最坏延迟：时段内 30 分钟 + 抖动 120 秒，跨时段则等到下一个时段」。
- **问候改为逐人分别生成**：原先 `send()` 是「先生成一段、再群发给所有对象」，
  每个人收到的问候一字不差；现在对**每个收件人单独生成**一份内容，
  节日祝福与日常闲聊同样如此。代价是对象越多，AI 调用次数与用量越大
  （配置 hint 与 README 都已写明）。某个对象生成失败（AI 失败且文案池为空）只跳过该人
  并记日志，不影响其他人；AI 不可用时仍回退文案池，每人随机取到的那条可能不同。
- **逐人生成时可用的个性化素材**（按优先级尽力获取，取不到就跳过）：
  1. **QQ 昵称**：优先用 OneBot 的 `get_friend_list` 一次取回全部好友昵称并缓存 6 小时
     （落盘 `nicknames.json`），缓存里没有该 QQ 时再用 `get_stranger_info` 补一次；
     都失败就不用昵称，称呼退化为「你」。
  2. **该用户的接收偏好**（来自 `user_prefs`）。
  3. **与他的最近往来**（来自已有的问候去重记录，例如「今天已经给他发过：早安」）。
  4. **该用户自己的备注**：新增配置 `greet_user_notes`（面板里是键值对/对象类型，默认空），
     形如 `{"10001": "同事，喜欢猫"}`；这是管理员主动提供的信息，只影响该对象收到的问候。
  5. **对方最近的说说**（新增 `greet_read_feeds` 默认开启、`greet_feed_count` 默认 2）：
     只取正文（忽略图片），逐条截断到 80 字，仅用于判断语气与近况、**不必与问候内容相关**；
     同一对象同一天只拉一次（进程内缓存），对象之间串行并留出 0.5~1 秒间隔，
     只在内存里使用、不落盘，日志最多写「已参考 M 条」；对方设了权限或不是好友时读不到，
     跳过该素材照常发送。
- **措辞安全约束写进默认提示词**（`greet_prompt`、`holiday_prompt`、`chat_open_prompt`）：
  只能用昵称作称呼或很温和的观察，禁止基于昵称联想调侃；禁止提及或暗示对方的外貌、性别、
  年龄、职业、地域、健康状况、经济情况与感情状态；禁止拿昵称里的自嘲、梗、数字、符号开玩笑；
  昵称含敏感词、广告、乱码或明显不是名字时一律不使用（退化为「你」）且不评论昵称本身；
  **禁止提到或暗示自己看过对方的说说**（不得出现「我看到你发的」「你最近发的那条」），
  也不得暗示「我在关注你」或「我知道你的一切」。
- **草稿确认兼容**：开启 `draft_for_greet` 时草稿里显示的是**预览**，
  并注明「确认后会按每个人分别生成内容再发出」；确认后确实逐人重新生成。
- **互动口径改为并集：特权名单 ∪ 已同意**。`interact_reply_uins` 的面板标题改为
  「特权名单」，语义调整为**并集**：名单内的人**无需任何同意**即可被自动评论 / 回复
  （含他的说说，以及他在自己说说下的评论与子回复）；名单外但接受过主动消息
  （用过 `/私聊开`）的人同样会被评论 / 回复；**两者都不是则不互动**。
  该口径**同时用于好友互动那条路径**：`interact_uins` 里只有满足口径的人才会被自动
  点赞 / 评论，其他人只读不打扰（不写去重记录，等对方以后接受或进名单后还能补上），
  被挡下的人数会写进巡检汇总的「名单/同意」一行。
  `interact_reply_require_optin` 保留、语义改为「是否启用这项检查」，**默认开启**
  （关闭即退回「谁都可以互动」）。`/空间回复` 与 `/空间状态` 会显示
  「回复对象：特权名单 N 人 ∪ 已同意 M 人（两者都不是则不互动）」。
- **名单类配置统一到「名单与权限」板块**：新增面板板块「名单与权限」（排在「基础设置」之后），
  按功能分组列出所有名单与同意设置——`interact_uins`（说说互动对象）、
  `interact_reply_uins`（评论回复特权名单）、`interact_reply_require_optin`（互动权限检查）、
  `greet_users`（主动消息对象）、`active_msg_require_optin`（主动消息同意）、
  `admin_uins`（确认与通知对象）；相应地从「基础设置」「私聊问候」「说说互动」里移出，
  这几处板块说明里注明名单在新板块。
  已同意名单本身是用户用 `/私聊开` 维护的运行时数据（不进面板），板块说明里写明可用
  `/空间状态` 查看人数。
  **升级不会丢名单**：旧板块位置保留同名隐藏副本（面板不显示），
  否则 AstrBot 加载配置时会按 schema 把用户已填的名单当成多余键删掉；
  插件启动时把这些值搬进新板块并删除旧位置，用 `_audience_keys_moved` 标记只搬一次。

### 修复

- **修掉「回复其实已经发出，却被判为失败、于是反复重发」的问题**。用真实账号直连
  空间接口复核后确认：回复接口 `emotion_cgi_re_feeds` **成功时返回的也是一段 HTML
  框架页**（`document.domain="…"; cb=frameElement.callback;`），不是 JSON；
  也就是说此前每一次回复其实都发出去了，只是插件把「回的是页面」当成失败，
  于是下一轮巡检又回了一遍，同一批评论下因此叠了好几层近似重复的回复。
- **成功与否改为「回查确认」，且判定放宽**：发出回复后回查一次说说详情
  （`emotion_cgi_msgdetail_v6`，`need_comment=1`、`need_private_comment=1`），
  在该评论（或子回复）的 `list_3` 里查找**任何一条来自我的回复**：找到即算成功并写入
  `replied_comments.json`；正文是否完全一致只写进日志（先按 `parent_tid` 精确归属，
  再退化为「该评论下有我的回复」，评论 id 没对上时再退化为整条说说里找）。
  这样即使清洗 / 截断让文本有细微差异，也不会因为判失败而重复回复。
  找不到才判失败。回复 POST 的回包既不算成功也不算失败。
- **未确认的回复不重复发（新增 `replied_attempts.json`）**：回查没确认时仍会留一条
  「已尝试」记录（键为带层级的候选 key，值是时间戳、原因与标识），
  **24 小时内不再对这条候选发起回复**，超过 24 小时才允许再试一次；
  确认成功后会清掉该记录。轮次内三重保险：先查带层级的去重记录 →
  再查结构化的「我的回复是否指向这一条」→ 再查已尝试记录，三者都通过才发 POST。
  `/空间回复` 与 `/空间状态` 会提示
  「有 N 条回复发出后没回查确认，24 小时内不会重发，可稍后到空间里人工确认」。
- **逐条日志写清处置结论**：每条候选写一行 `[reply] 说说tid/层级标识：…`（例如 `P/c1`、`P/c1/r2`），
  取值包括 `跳过（已回复）`、`跳过（已有我的回复）`、`跳过（已尝试未确认，24 小时内不重发）`、
  `跳过（不在特权名单且未同意）`、`跳过（自己发的）`、`已回复（回查命中）`、
  `失败（保留尝试记录）`，一眼能看出没有重复发。
- **修掉「对方回复我的回复时被误判为已回复而跳过」的问题（层级身份）**：
  实测确认空间给**父评论与子回复各自独立编号**（都从 1 开始，用户那条帖子下
  父评论 tid=1、子回复 tid 也从 1 起），而此前的去重键 `{说说tid}_{候选tid}`
  和按 tid 数字做的归属判断都**没有层级信息**，于是父评论 tid=2 与子回复 tid=2
  会互相冲突，对方新发的子回复被判成「已回复」而跳过。现在：

  - 候选身份带上层级：顶层评论 `{说说tid}_c{评论tid}`，子回复
    `{说说tid}_c{父评论tid}_r{子回复tid}`（更深一层继续拼 `_r{tid}`），
    `replied_comments.json` 与 `replied_attempts.json` 都用新键；
    老键仍能读（只用于顶层评论候选，避免又按裸 tid 撞号）；
  - 日志里的候选标识带层级（`c1`、`c1/r2`），不会再出现两条都叫 `1` 分不清；
  - **发送前检查结构化**：只有「我的回复的 `parent_tid` 等于该候选的 tid，
    且该回复的时间不早于候选」才算「我已经回过这条」；不再用「这条评论的
    `list_3` 里有我的回复」这种宽松依据（那正是误杀新子回复的原因），
    归属判断不了时只信精确 key；
  - **发送后回查以 POST 时刻为锚**：只认该候选所在线程里、`create_time` 不早于
    发出请求前 120 秒的我的回复，不再退化成「整条说说里任何一条我的回复」
    （那会把历史回复当成本次成功）；
  - 解析层保留完整层级：父评论 tid、子回复自己的 tid、接口没给时填成父节点 tid 的
    `parent_tid`，并递归解析更深一层（回复的回复），最多 4 层。
- **去重精确到「具体哪一条」，不再整条线程一起跳过**：此前只要线程里出现过我的回复，
  整条线程（父评论 + 别人后续的子回复）都会被跳过，结果是**对方继续回复时不会被回应**。
  现在逐个候选判断（身份带层级，见上一条）：这一条已在去重记录里，
  或者我的回复明确指向这一条才跳过，其余一律放行；
  同一线程里的候选按**时间倒序**处理，「对方刚回的那条」优先，
  仍受每轮上限与「同一条说说每轮最多回一条」约束。
- **解析子回复并保留层级**：评论对象解析 `FeedComment.replies`
  （字段名 `list_3` 为主、`list` 也认，子回复的 `parent_tid` 指向父节点 tid），
  别人追加的子回复会被当作新的待回复对象，回复它时 `commentId` 用那条子回复自己的 tid。
- **回复时结合整段对话**：此前只把最后一条评论交给 AI，回答孤立、还容易重复自己
  说过的话。现在生成回复会把整段交流一起给出——说说正文（截断到 80 字）、父评论、
  该线程下全部子回复（按时间升序、标明说话人；我自己的回复标成「我」，
  并单独列一段「我之前已经说过的话」），最后明确写出本次要回复的是哪一条。
  默认提示词与生成时的固定要求都写明：结合整段对话回应对方最新那条、
  不要只针对最后一句孤立作答、不要重复自己已经说过的意思、
  不要提及自己在查看评论记录。
- **「返回的是页面」不参与成功判定**：框架页（`frameElement.callback` /
  `document.domain`）仍记作 `-3002`，但只用于日志与提示，结论一律由回查给出；
  回复路径的这类页面只写 debug 日志，不再按错误记录。验证 / 风控页单独记为
  `-3005`，HTTP 403 记为 `-3003`，这几类都**不重取登录态、也不重试**。
  只有确实返回登录页（`ptlogin` / 请先登录）、HTTP 401 或业务码 `-3000`
  才判定为登录失效并重取登录态重试一次——登录页特征也因此收窄，
  不再使用「登录」「login」这类会误伤框架页的宽泛字符串。
- **回复请求不再带 h5 专用请求头**：与 `comment()` 完全一致，接口地址也统一到
  `user.qzone.qq.com` 域（实测 h5 域也能通，统一后少一个变量）。
- **失败日志补齐排查要素**：写出一行
  `回复评论失败: url=…｜topicId=…｜commentId=…｜commentUin=…｜原因=…｜回查结果=…｜响应片段: …`
  （响应片段取正文可读部分前 300 字），确认成功时同样记一行「回复评论已确认」。
- **评论 id 校验放宽**：此前把「不足 6 位的纯数字 id」当可疑 id 跳过，而实测空间给
  评论的 id 就是小整数（例如 `1`），用它能正常回复成功——这条规则会把这类评论
  永久跳过，属于比原问题更严重误杀。现在只挡空 id 与和说说 id 相同的解析错位 id，
  其余一律放行；短数字 id 只在日志里留一行 debug。

### 安全

- **指令权限收紧**：`/空间状态` 与 `/空间历史` 改为仅管理员可用——前者会显示管理员名单、
  登录态、模型提供商、Token 用量与草稿情况，后者会显示发布历史与失败原因，都属于内部信息。
  现在除 `/空间日程`、`/私聊开`、`/私聊关` 三条外，**所有指令都限管理员**；
  并新增自测解析 `main.py` 的指令装饰器，保证这条口径不被以后新增的指令破坏。

### 说明

- 新增配置 5 个：`greet_read_feeds`（bool，默认 `true`）、`greet_feed_count`
  （int，滑块 1~5，默认 `2`）、`greet_user_notes`（键值对/对象类型，默认空），
  均在「私聊问候」板块；以及 `interact_reply_uins`（列表，默认空，「评论回复特权名单」）
  与 `interact_reply_require_optin`（bool，**默认 `true`**），
  在「名单与权限」板块。
- 面板板块由 7 个改为 8 个：新增「名单与权限」（第 2 个板块），
  原来分散在「基础设置」「私聊问候」「说说互动」里的 6 个名单 / 同意类配置统一搬过去。
- 新增数据文件 `replied_attempts.json`（发出后没回查确认的回复，24 小时内不再重发）。
- 新增数据文件 `nicknames.json`（好友昵称缓存，6 小时刷新一次，原子写）；
  对方说说的正文只在内存里使用，不落盘。
- 自测 **656 → 770 项**：其中 `[39]` 与时段相关的 43 项包括——回复巡检时段解析（默认值、12:00 / 13:30 / 20:00 / 23:30 是巡检点，11:30 / 14:30 / 19:30 / 次日 00:00 不是:30 不是，且给出各自的下一个时段首轮）、「23:40 → 次日 12:00」等跨时段下一轮、状态文本里的时段与下一轮，以及昵称清洗规则（正常 / 广告 / 纯数字纯符号 /
  超长 / 敏感词）、昵称缓存落盘与 TTL 内不再重复拉取、缓存缺失时用陌生人接口补一次、
  接口失败退化为不使用昵称、逐人生成（两人内容不同、每人各一次 AI 调用）、
  昵称与备注进入对应对象的提示词且备注不外泄、偏好与最近往来进入提示词、
  对方近况进入提示词并写明「不必相关」与「不得提及看过对方说说」、
  默认提示词本身含约束、每个对象各拉一次说说且条数符合配置、对象之间串行并留出间隔、
  同一对象同一天只拉一次、关闭开关后不发该请求、正文逐条截断 80 字、
  读不到对方说说时照常发送、某个对象生成失败只跳过该人且不影响他人；
  同时把回复巡检相关的 6 处断言同步到新的默认间隔与文案。
- 自测新增 `[41]` 共 64 项：核对源码里回复接口走的是 `user.qzone.qq.com` 域、
  与评论接口一致；回复只回 HTML 框架页但回查能在子回复里找到自己的回复 → 判定成功、
  写去重且不产生任何失败日志/错误回执；**回查只命中「自己的回复但正文略有差异」
  也判成功、不重复发**；**回查只认 POST 之后新增的回复（把历史回复放在 `list_3`、
  时间早于 POST → 判失败并留尝试记录）**；框架页且回查找不到 → 判定失败、
  不重取登录态也不重试、不写去重，并留下 `replied_attempts.json` 尝试记录、
  同一候选下一轮不再重发（POST 仍只有 1 次），回拨时间超过 24 小时后允许重试一次
  并清掉记录；**层级身份：父评论与子回复同号（都为 1）时不互相误判——
  父评论跳过、对方新发的子回复被回复，key 分别是 `P_c1` 与 `P_c1_r1`，
  日志标识为 `c1` 与 `c1/r1`；回复过的新子回复下一轮不再回；
  老格式去重键仍能读、新写入用带层级的新键**；评论下已有针对**这一条**的我的回复 →
  跳过且一次请求都不发；**线程里已有我的回复时，对方新发的子回复仍会被回复**；
  同一线程多个未回复候选时**最新优先**；别人的子回复会被回复，
  且 `commentId` / `commentUin` 用子回复自己的；子回复字段兼容 `list_3` 与 `list`
  且 `parent_tid` 指向父评论；回复提示词里确实含说说正文、父评论、全部子回复
  （升序）、「我之前已经说过的话」一段与「本次要回复的是哪一条」，
  生成要求含「不要只针对最后一句」「不要重复」；**并集口径四种组合（在特权名单未同意 /
  不在名单已同意 / 在名单也已同意 / 两者都不是）各自的回复与跳过行为**、
  状态里的「特权名单 N 人 ∪ 已同意 M 人」与 `/空间回复` 的对应行、
  **好友互动的自动评论同样遵守该口径（含特权名单免同意）**、
  逐条处置日志（`跳过（已回复）` 等）；回复请求头与评论接口逐项一致
  （未使用 h5 专用请求头）；登录页 / `-3000` / 401 三类仍自动重取登录态并重试一次
  且回查确认成功；403 单独文案且不重登不重试；失败日志含接口地址、topicId、
  commentId、commentUin、响应片段与回查结果；评论 id 校验（空 id 与和说说 id 相同的挡下，
  `1` 与 `2` 这类短数字 id 放行且确实发出请求）。

## [2.10.1] - 2026-09-23

### 变更

- 评论回复不再经过草稿确认：`draft_for_reply` 选项已从配置面板移除，回复在巡检到时直接发出；
  历史遗留的回复草稿会在插件启动时自动丢弃并记日志，不会再推送给用户。
- 自测 656 项：删除 5 项回复草稿断言，新增「回复不产生任何草稿」「配置对象与面板均不含该选项」
  「回复状态里不出现草稿字样」等 5 项。
## [2.10.0] - 2026-09-24

### 修复

- **别人评论了自己的说说却没有回复**：根因有三处，均已修正。
  - **回复巡检独立成高频任务**：原先回复寄生在 `interact_cron`（默认 `0 21 * * *`）
    上，一天只跑一次，「有人评论就回」根本做不到。新增配置 `interact_reply_cron`
    （默认 `*/5 8-23 * * *`，白天到晚上每 5 分钟一轮）与 `interact_reply_jitter`
    （默认 60 秒），任务名 `qzone_reply`，受 `interact_reply_enabled` 控制；
    `/空间回复 on|off` 会即时重建或停止该任务。QQ空间没有评论推送通道
    （OneBot 只推送 QQ 消息事件），评论只能靠轮询发现，因此把间隔做小并做成可配置。
  - **回复有了自己的时间窗口**：原来看的是 `interact_days`（默认 3 天），
    导致旧说说下面的新评论永远发现不了。新增配置 `interact_reply_days`
    （int，滑块 1~30，默认 7），回复巡检只用它判断窗口。
  - **回复一律直接发出，不再走草稿**：删除了「回复评论也先走草稿确认」这个配置项
    （`draft_for_reply` 从 schema 中移除，AstrBot 加载时会丢弃该键），
    同时删掉了「已有待确认回复草稿就跳过该评论」的占用逻辑与回复草稿的处理分支；
    回复巡检到新评论即直接发出，也不再给用户看回复草稿。旧版本遗留的回复草稿会在启动时
    被丢弃，用 `/空间重写` 处理这类草稿会提示它已失效。
- **可观测性**：每轮巡检无论有没有新评论都写一行 info 日志
  （`[reply] 第 N 轮评论巡检：检查 … 回复 … 转草稿 … 跳过 …`）；
  发现新评论却未回复时写明原因（例如「本轮达到每轮上限 N 条，剩余新评论会在下一轮
  继续处理」）。
- `/空间回复` 无参数时显示：开关、巡检间隔（含人话说明与 Cron）、时间窗口、
  每轮上限、**今日已回几条**、**下一次巡检时刻**，并说明
  「QQ空间没有评论推送，评论只能靠定时轮询发现」。
- `/空间状态` 的「评论回复」一行补上「巡检 <cron>｜窗口 N 天」。
- 新增 `reply_counts.json`（每日回复条数，保留 7 天，原子写），用于「今日已回几条」。

### 变更

- **统一所有用户可见回执的排版**（新增 `core/ui.py`，其余模块改为调用它）：
  指令回执、发布通知、草稿描述、巡检汇总、问候与闲聊回执、错误提示与 `/空间状态`
  输出都改为「符号 +【标签】」的区块标题（`✅【发布成功】`、`⚠️【需要注意】`、
  `❌【操作失败】`、`📌【状态】`、`📌【草稿待确认】`），区块内统一使用
  `  · 名称：值` 的键值行，多区块输出（`/空间状态`）用 `────────` 分隔线隔开。
- **一份内容两种渲染**：QQ 纯文本消息不渲染 Markdown，因此纯文本版只用能在 QQ 里
  正常显示的字符（【】标签、「」引号、`·` 与缩进、树形线、分隔线与四个状态符号
  `✅ ⚠️ ❌ 📌`），不含任何 Markdown 标记；回执图（t2i）路径使用同一份内容的 Markdown 版，
  【】标签成为加粗标题、键值名称成为真加粗。`_notify` 与 `_send_draft` 在渲染图片时
  传 Markdown 版，纯文本发送时用纯文本版；渲染失败仍降级为纯文本。
- **长度约束**：单条回执纯文本不超过 12 行，`/空间状态` 按区块拆分且每个区块不超过 8 行，
  超出时截断并补一行省略提示（完整内容可由回执图承载）。
- **指令提示写法**：指令名统一为「/空间发布」这种直角引号写法，一行内最多给出两条指令。
- `/空间状态` 由原来的平铺长列表改为区块结构（登录与接入 / 生成与回执 / 定时任务 /
  草稿确认 / 定时问候 / 节日祝福与闲聊 / 主动消息与最近发布），区块之间一条分隔线。
- 用量提示从「📊 …」改为区块形式（`📌【用量估算】` + 键值行），符号收敛到规定的四个。
- **AI 生成内容不受影响**：说说正文、评论、回复与问候内容不参与这套排版，按原样发布。

### 说明

- 本次新增 3 个配置项（`interact_reply_cron`、`interact_reply_jitter`、
  `interact_reply_days`，均在「说说互动」板块），并**移除了 1 个配置项**
  （`draft_for_reply`）：回复评论不再走草稿确认，回复一律直接发出。
  该键会从用户配置中被 AstrBot 丢弃，属于配置项精简。
- 关键信息在文案调整中被保留（`发布成功`、`tid`、`发送地址`、`（已同意）`、`草稿`、
  `用法`、`巡检完成`、`评论回复完成`、`主动闲聊` 等）；受影响的既有断言按新文案同步更新，
  断言意图不变（例如状态行的冒号由半角改为全角、`图片: 1 张` 改为 `图片：1 张`）。
- 自测 **607 → 635 项**：新增 `[37]` 共 28 项断言——状态符号只有四个、分隔线为 8 个破折号、
  单条回执与区块的行数上限、区块标题形式、键值行形式、Markdown 版的标题与键名加粗、
  纯文本版不含 Markdown 标记、多区块插入分隔线、两版内容一致（只有标记不同）、
  区块截断与不截断、指令名直角引号与一行最多两条、`plain()` 降级、键值占位，
  以及实际发布回执 / 草稿描述 / 状态输出 / 用量提示 / 首次引导的排版校验。
- 自测合计 **607 → 656 项**：除 `[37]` 的 28 项外，另新增 `[38]` 与窗口、回复草稿移除相关的断言——
  回复任务按 `interact_reply_cron` 独立调度（任务名 `qzone_reply`、抖动、与好友巡检互不影响）、
  间隔人话说明、回复窗口默认 7 天 / 好友互动默认 3 天、
  5 天前的说说在默认窗口内会被处理（调成 3 天后不再处理）、空轮次也写巡检日志、
  达到每轮上限时日志写明原因、`/空间回复` 开关即时重建与停止任务、
  `/空间回复` 与 `/空间状态` 的新字段、`now` 立即执行不改开关、
  **回复一律直接发出且不写任何草稿文件**、**配置项与状态输出里都不再出现该键**；
  原先「回复草稿」相关的 5 项断言随功能移除一并删除。

## [2.9.0] - 2026-09-24

### 修复

- **发布说说偶发「JSON 解析失败」**：QQ空间返回的并非常规 JSON，失败时还可能直接回一整页
  HTML（登录页 / 风控页）。解析层改为按「逐级放宽」的顺序尝试（`core/qzone/parser.py`）：
  1. 直接 `json.loads`；
  2. 剥离 JS 包裹（`_Callback(...)`、`frameElement.callback(...)`、多余分号与空白，原有逻辑保留）；
  3. 放宽写法后再解析：单引号字符串改双引号、给无引号的键加引号、去掉尾逗号；
  4. 仍失败时区分原因：命中 HTML / `ptlogin` / 登录 / 验证 / 安全 / 风控等特征就判定为
     **登录态失效或被风控**（合成码 `-3001`），否则报「响应格式无法识别」。
  两种情况都会用 `logger.error` 记录**响应前 300 字符**（换行与制表符可见化），
  以后可直接判断是格式变化还是风控；放宽解析只在直接解析失败后使用，不会破坏正常响应。
- **失败自动重取登录态并重试一次**：传输层（`core/qzone/client.py`）把「401 / `-3000` /
  解析层判定的 `-3001`」统一视为需要重登，`session.invalidate()` 后重试一次；
  发布、点赞、评论、回复、详情等路径共用这条逻辑。重试后仍失败才回报失败，
  错误文案为可操作提示（登录态可能已失效或被风控拦截，已自动重取登录态后仍然失败，
  请用 `/空间重登` 重取后再试）。
- **回执写清原因**：登录 / 风控类失败时回执给出「登录态可能已失效或被风控拦截，请用
  `/空间重登` 重取登录态，或稍后重试」；原始格式无法识别时回执为「响应格式无法识别
  （已记录响应片段，详见日志）」。原来的「JSON 解析失败」不再作为泛化文案出现。

### 新增

- **避免每天的说说内容重复**（`core/content.py`，三层机制一起生效）：
  1. **参考最近发过的内容**：新增配置 `publish_avoid_repeat_count`（int，滑块 0~20，默认 5）：
     把最近这么多条发布成功的正文（逐条截断到 60 字，取发布历史里的成功记录、最新在前）
     作为「最近已经发过的内容（不要重复）」交给 AI，并要求不要重复用过的意象、句式、
     开头方式与结尾方式；`0` 表示不参考。
  2. **创作角度与当前时段**：新增配置 `publish_angle_pool`（list，默认 7 条具体角度）：
     每次随机抽一个角度注入提示词，并记录当天已用过的角度（落盘 `publish_angles.json`，
     原子写、保留 7 天），**保证同一天内各条角度不同**（池子当天用尽时才允许重复并写日志）；
     同时把**当前时段**（早上 / 中午 / 下午 / 晚上 / 深夜，按插件时区）告诉 AI，
     要求只写当下这个时段的事、不写一整天。
  3. **相似度把关**：新增配置 `publish_repeat_threshold`（int，滑块 0~100，默认 60）：
     生成后与最近内容按字符 bigram 的 Jaccard 相似度比较，达到阈值就**自动重写一次**，
     重写时在提示词里点明「与最近某条过于相似，换一个完全不同的切入角度」；
     重写后仍然相似则照常发布，并在日志、发布通知与 `/空间状态` 中注明；`100` 表示关闭检查。
- **状态可见**：`/空间状态` 的「上次生成依据」新增本次的**创作角度**与**时段**，
  并新增一行「避免重复」（参考条数、与最近内容的相似度、是否触发过重写）。
- 发布成功通知与 `/空间自动发` 回执会在「重写后仍相似」时附上提醒。

### 说明

- 新增的 3 个配置项都放在「空间说说」板块，按该板块既有顺序归类：
  `publish_angle_pool` 归入「内容与提示词」，`publish_avoid_repeat_count` 与
  `publish_repeat_threshold` 归入「数量与限制」；板块顺序与 65 个隐藏迁移项均未变动。
- 自测 **562 → 607 项**：新增 45 项断言——响应解析容错（单引号键、无引号键、尾逗号、
  JS 包裹、单引号串内的双引号与逗号不被误改、原有 JSONP 与 undefined 行为回归）、
  HTML / ptlogin / DOCTYPE 风控页判定与可操作文案、非登录页错误文案、响应片段可见化与
  **确实写进日志**、登录页响应触发一次重取与重试、重试后仍失败的回报文案、
  格式错误时回执指向日志且写入发布历史；以及避免重复（时段划分、相似度的三种典型取值、
  最近内容只取成功记录且倒序、逐条截断 60 字、参考条数 0 的行为、角度同一天不重复与落盘重载、
  角度池用尽、提示词包含最近内容 / 角度 / 时段、相似度超阈值自动重写一次并点明原因、
  重写后仍相似时照发并写日志与回执注记、阈值 100 关闭检查、状态输出）。

## [2.8.0] - 2026-09-24

### 新增

- **日常闲聊（定时主动开口）**（`core/greet.py`，沿用问候的去重文件 `greet_state.json`）：
  在配置的时间窗口内随机挑一个时刻，主动给一位已同意的用户发一两句轻松的搭话。
  新增配置项（全部位于「私聊问候」板块）：
  - `chat_open_enabled`（bool，**默认关闭**）：总开关。
  - `chat_open_windows`（list，默认 `["12:00-13:30", "19:00-22:00"]`）：时间窗口，
    格式 `HH:MM-HH:MM`，支持跨零点（如 `23:00-01:00`）；无法识别的项被忽略并写 warning
    日志，其余窗口照常生效。
  - `chat_open_per_day`（int，滑块 1~3，默认 1）：每天最多主动开口几次；从可用窗口里
    随机挑这么多个窗口各发一条，窗口个数不足时按窗口数发送。
  - `llm_chat_open_provider_id`（string，`select_provider`，默认空）：留空用全局提供商。
  - `chat_open_prompt`（text）：搭话用提示词，默认值自带语气约束——轻松、不冒犯、
    不打探隐私、不用「在吗 / 干嘛呢」这类空招呼、一到两句话、给对方留出不理会的余地、
    可自然结合当日日程。
  - `chat_open_max_chars`（int，滑块 10~100，默认 40）：搭话字数上限。
- **随机时刻与去重**：每个窗口对应一个调度任务，触发时刻固定在窗口起点，随机抖动上限设为
  该窗口长度，因此实际发送时刻落在窗口内且每天不固定；触发时会再次确认当前时刻仍在窗口内
  （允许 10 分钟补偿宽限），窗口已过则跳过。窗口数多于每天条数时按日期随机挑选窗口
  （同一天结果固定，重启后不变）。去重键为 `chat:<日期>:<QQ>`，因此**同一个人每天最多
  收到一条**，全局每天不超过 `chat_open_per_day` 条，且每次只发给一个人。
- **内容生成**：AI 结合 AstrBot 全局人格与当日生活日程生成，按字数上限截断并清洗引号与空白；
  AI 失败或返回为空时回退内置的三条温和文案（内置常量，不占配置项）。
- **新指令 `/空间闲聊 [on|off|now]`**（别名 `space chat` / `qz chat`，仅管理员）：
  不带参数显示开关、时间窗口、每天上限、今日已发人数与下一个窗口；`on` / `off` 开关并即时
  重排任务；`now` 立刻发一条测试（忽略时间窗口与当日去重，仍遵守主动消息同意设置），
  回执里给出实际发送地址。
- **用户侧开关**：`/私聊开` 与 `/私聊关` 的参数新增「闲聊」（同时支持英文 `chat` 与
  「聊天」「日常闲聊」），偏好文件里新增功能键 `chat`（展示名「日常闲聊」）；
  首次私聊引导的功能清单加入日常闲聊，并写明「白天与晚上可能收到一两句招呼」。
- **状态**：`/空间状态` 新增一行「主动闲聊」，显示开关、时间窗口、每天上限、今日已发人数
  与下一个窗口时刻。

### 说明

- **主动闲聊不走草稿确认**（与问候的 `draft_for_greet` 无关）：它是私聊内容，且收件人已经
  明确同意接收，多一次人工确认往往会错过闲聊的时机；该取舍同时写在 `main.py` 的调用处
  注释与 README 中。
- 同意门控与早安 / 晚安、节日祝福一致：`active_msg_require_optin` 开启时只发给已接受且
  没有单独关掉「日常闲聊」的用户；关闭时退回按 `greet_users` 发送。
- 「私聊问候」板块内部顺序仍为「开关 → 对象与时间 → 模型提供商 → 内容与提示词 →
  数量与限制」，新增的 6 项按类别插入对应位置。
- 自测 **502 → 562 项**：新增主动闲聊 60 项断言——窗口解析（合法、多种非法写法、跨零点、
  窗口内 / 外判定、补偿宽限）、每个窗口一个任务且 cron 为窗口起点、抖动上限等于窗口长度、
  注入时刻后随机时刻落在窗口内、当天窗口挑选（结果固定与窗口不足）、窗口已过跳过、每天上限、
  同一个人每天一条、同意门控（未同意不发、单独关掉闲聊不发、关闭同意机制后退回对象列表）、
  AI 生成与提示词内容、字数上限与引号清洗、AI 失败与空返回回退内置文案、
  `/空间闲聊` 四种行为、状态输出、用户侧开关与引导文案。

## [2.7.0] - 2026-09-23

### 新增

- **私聊用户偏好与首次引导**（`core/user_prefs.py`，落盘 `user_prefs.json`）：区分不同私聊用户，
  每人一份偏好。用户第一次私聊机器人时会收到一条不超过 6 行的说明（列出可能的时间段、
  可能打扰到的功能，并写明「不回应视为不接受」）；指令、唤醒前缀开头的消息与群聊都不会触发这条引导。
  新增配置 `active_msg_require_optin`（bool，**默认开启**）：开启后只有明确接受过的用户才会收到
  早安、晚安与节日祝福，未接受的人被跳过并计入「因未接受主动消息跳过 N 人」；关闭则恢复旧行为。
  **收件人口径三者统一**：同意机制开启时，早安 / 晚安 / 节日祝福分别发给
  `prefs.allowed_users("morning" | "night" | "holiday")`（已接受且没有单独关掉该项的用户），
  **不再读 `greet_users`**；同意机制关闭时统一退回按 `greet_users` 发送、不检查偏好。
  `greet_users` 因此保留但语义收窄为「仅同意机制关闭时生效」。管理员手动测试
  （`/空间问候 morning <QQ>`、`/空间问候 holiday [QQ]`）是显式指定对象的动作，不受偏好限制。

  新增两条用户侧指令（**所有用户可用**）：`/私聊开 [早安|晚安|节日]` 与 `/私聊关 [早安|晚安|节日]`，
  不带参数即全部开启 / 关闭，带参数只开或只关该项；回执会显示当前状态、各功能开关与管理员配置的
  时间段（只读）。**参数无法识别时只提示、不改动状态**，尤其不会因此变成「已接受」。
- **每天多条发布**：新增配置 `publish_per_day`（int，默认 1，0 表示不发布）与
  `publish_times`（list，**默认空列表**，可填多个 `HH:MM`）。调度改为 `CronTaskGroup`：
  每个时间点一个独立任务（名为 `qzone_auto_publish[1]`、`[2]`…），各自计算下次触发时间与随机抖动，
  改配置后整体重建完成热更新。`publish_cron`（默认 `30 8 * * *`）仍是「每天一次」的时间来源：
  `publish_times` 留空时按它执行，所以**开箱行为与旧版一致（08:30）**。
  `/空间定时` 支持 `08:30,12:30,21:00`、`每天 2 08:30,12:30`、单个 Cron 与 `off` 四种写法。
  **升级兼容**：旧版 `publish_cron` 会被继承到新机制，发布时间不会被静默改成默认值——
  「每天一次」的简单写法（`HH:MM`，或 5 段中 分/时为单值且 日/月/周 都是 `*`，例如 `30 00 * * *`）
  会转成 `publish_times=["00:30"]` 并保持 `publish_per_day=1`；带星期/月份限定或多值的复杂写法
  （例如 `0 8 * * 1`、`0 9,18 * * *`）不转换，留空 `publish_times` 由 `publish_cron` 兜底。
  两种情形都会写 INFO 日志说明继承结果。
  **条数多于可用时间点个数时视为配置不完整**：当天不自动发布、不创建多余任务，并在
  `/空间状态`、`/空间定时` 回执与日志里提示「publish_per_day 为 N，但 publish_times 只有 M 个
  可用时间点，请补齐或调小条数」。
- **节日祝福**（`core/holidays.py`）：支持 7 个节日——农历的除夕、春节、元宵、七夕、中秋
  （公历日期覆盖 2026-2030 年，逐条对照香港天文台《公曆與農曆對照表》核对，来源写在文件开头），
  以及固定公历的情人节（2 月 14 日）与国庆（10 月 1 日）。
  农历节日在覆盖范围外的年份写 warning 日志提醒更新，不会静默失效。
  新增配置 `holiday_enabled`（**默认关闭**）、`holiday_cron`、`holiday_jitter`、
  `llm_holiday_provider_id`、`holiday_prompt`（`{festival}` 占位符）、`holiday_pool`；
  当天不是节日不发送，同一节日只发一次，AI 失败回退文案池。
  **收件人口径**：与早安 / 晚安一致（见上），即同意机制开启时只发给「已同意接收节日祝福」的用户；
  `/空间状态` 与 `/空间问候` 都会显示「本次将发给 N 人（已同意）」，无人同意时明确提示
  「目前没有已同意接收……的用户，可在私聊里回复 /私聊开 接受」。
  是否先转草稿沿用 `draft_for_greet`。`/空间问候 holiday` 可测试发送。

### 变更

- `/空间状态` 新增「节日祝福」（含下一个节日与日期、今日已发人数）与「主动消息同意情况」两行，
  定时发布一行改为显示「每天 N 条：时间点列表」。
- 问候相关通知在有人因未接受被跳过时，也会把这一项写进汇总。
- 自测 **352 → 502 项**：新增用户偏好存储与语义、首次引导（只发一次 / 仅私聊 / 跳过指令与群聊）、
  两个用户侧指令的全部用法与「参数无法识别不改状态」、未接受者被跳过与关闭开关后的对照、
  统一收件人口径（早安 / 晚安 / 节日祝福各自只发给已同意且未关掉该项的人，
  `greet_users` 里填了未同意者也不发；关闭同意机制后按 `greet_users` 全发；
  手动指定 QQ 时不受偏好限制；无人同意时状态与通知给出提示）、
  多时间点调度（条数截断、配置不完整时不发布并提示、回退兼容项、非法项处理、抖动透传、指令四种写法）、
  升级迁移的发布时间继承（`30 00 * * *` → `publish_times=["00:30"]` 且调度按 00:30；
  `0 8 * * 1` 保持 `publish_times=[]` 并由 `publish_cron` 触发、状态里显示原 cron；
  两处都为空时不发布且不报错；新用户开箱为 `publish_cron=30 8 * * *` 即 08:30）、
  节日日期表（5 个农历节日 × 5 年逐条断言、情人节与国庆固定日期、非节日、农历表范围外）、
  节日发送（当天发送、只发一次、非节日不发、手动测试、AI 失败回退、指令入口）等断言。

### 文档

- README 重新组织结构，固定为 10 段：标题与徽章 / 一句话简介 / 功能 / 安装（含前置条件）/
  使用说明 / 指令 / 功能说明 / 配置说明 / 目录结构与数据文件 / 免责声明。
  「使用说明」由原来的「快速开始」扩写为分步上手流程（几步上手、典型命令，并新增
  「结果在哪里看」一节说明发布通知、历史、草稿、状态与巡检结果的查看位置）；
  标题与正文不再使用表情符号。
- 删除 4 章：「常见问题」「工作原理」（含「生成链路」这类内部机制说明之外的部分）、
  「开发与测试」「致谢与许可」。其中：
  - 排查说明（定时问候收不到、说说不发布、联网素材不生效、草稿与通知发不出去等）
    并入「功能说明」对应小节，不再单独设立排查章节；协议实现细节以
    [THIRD_PARTY_NOTICES.md](THIRD_PARTY_NOTICES.md) 的声明为准；
  - 开发与测试的执行方式见 [tests/README.md](tests/README.md) 与
    [.github/workflows/ci.yml](.github/workflows/ci.yml)，README 不再重复。
- `LICENSE`、`THIRD_PARTY_NOTICES.md` 文件保留在仓库中，README 不再单开章节；
  许可信息仍由徽章与目录结构一节指向这两个文件。
- 「目录结构与数据文件」改为用户视角：只列插件目录内用户见得到的文件（入口、元数据、
  配置面板、图标、文档、许可）与运行期数据文件（补齐 `token_usage.json`），
  并说明「其余为插件内部实现与自测脚本」，不再逐个列出 `core/`、`tests/`、`tools/` 等研发侧条目。
- 删除「功能说明」下的「内容生成链路」小节（属内部机制），其中用户可见的两点
  ——生成时参考 AstrBot 中选定的人格与当日生活日程、日程缺失时退化为按身份临时安排——
  压成两句话并入「定时发布与内容来源」。

## [2.6.0] - 2026-09-23

### 新增

- **回复自己说说下的评论**（`interact_reply_enabled`，默认关闭）：自己发布的说说下面
  有人评论时，用 AI 生成一句回复并通过空间接口发出。内容只由 AstrBot 提供商生成，
  可用 `llm_reply_provider_id` 单独指定模型、`interact_reply_prompt` 调整提示词、
  `interact_reply_max_chars`（默认 80）限制字数。
- **不重复的三道限制**：同一条评论只回复一次（`说说tid_评论tid` 记录在
  `replied_comments.json`，且只有回复真正发出后才写入）；自己的评论与空内容评论直接跳过；
  每轮最多回复 `interact_reply_max_per_run`（默认 3）条，同一条说说每轮最多回一条。
- **默认先确认**：新增 `draft_for_reply`（默认开启），回复先作为 `reply` 类型的草稿
  发给你，用 `/空间确认` 后才真正发出；草稿被放弃时不计入已回复，下次巡检会重试。
- **新指令 `/空间回复 [on|off|now]`**（别名 `space reply` / `qz reply`）：查看状态、
  开关回复功能，或立即执行一轮巡检；`/空间读说说` 与定时巡检也会一并检查回复。
- **协议层**：新增 `FeedComment` 模型与 `commentlist` 解析、`api.reply()`（回复评论）与
  `api.get_detail()`（说说列表未带评论明细时补取详情），接口参数沿用已知的空间网页端协议。
- 自测 324 → **352 项**（新增评论解析与容错、跳过规则、去重落盘、每轮上限、草稿确认与指令行为等 28 项）。

### 说明

- 只回复**接口能取到的文字评论**：纯图片、纯表情评论，以及列表与详情都取不到明细的说说会被跳过。
- 回复公开可见，因此功能默认关闭、且默认走草稿确认。

## [2.5.0] - 2026-09-23

### 变更

- **配置面板按板块重排**：原来 64 个配置项平铺在面板上，现在分成 7 个带标题的板块——
  基础设置 / 私聊问候 / 空间说说 / 说说互动 / 生活日程 / 草稿确认 / 网络与登录。
  每个板块内部统一按「开关 → 对象与时间 → 模型提供商 → 内容与提示词 → 数量与限制 → 通知」
  排列，四处模型提供商选项因此都固定在各板块的同一相对位置。
- **升级不会丢配置**：旧版扁平配置会在首次启动时自动搬迁到对应板块（值不变，只换位置），
  面板里不需要重新填写。README 的配置说明同步改成与面板一一对应。

### 内部

- `core/config.py` 新增「配置项名 → 板块路径」索引，业务代码仍按扁平名字读写配置，
  面板排版与代码彻底解耦；新增 `PluginConfig.migrate_flat_config()`（带迁移标记，只执行一次）
  与 `section_of`（配置项 → 板块标题）。
- 旧扁平键在 schema 中保留为带条件的隐藏项，AstrBot 加载时不会把它们当多余键删除；
  迁移完成后这些隐藏项只是占位。
- 自测 **314 → 324 项**：新增板块数量与顺序、板块标题非空、提供商与提示词的相对顺序、
  隐藏迁移项数量、旧配置搬迁、迁移后仍可读取、二次启动不用旧值覆盖新值等断言。

## [2.4.1] - 2026-09-23

### 修复

- **定时问候「日志说成功、实际没收到」**（两个成因都修了）：
  - `/空间问候 morning <QQ>` 手动测试原本会写入 `greet_state.json` 的「今日已问候」记录，
    于是当天的**定时问候到点后被判定为已发而直接跳过**（日志表现为「成功 0 人，跳过 1 人」，
    看起来像是发过了）。现在只有定时任务写这条记录，手动发送不占当日名额，并在回执里注明。
  - 发送结果不再被当成成功：插件会核对 AstrBot `send_message` 的返回值，
    没找到匹配会话时记为失败、写明原因与实际 umo，且**不写「已问候」记录**，
    下次定时仍会重试；连平台实例都找不到时直接报错，而不是照样记成功。
  - 问候优先使用「最近一次与该 QQ 的真实私聊会话地址」（AstrBot 自己用的 umo），
    拿不到时才按平台实例 id 拼装，避免多平台或改过实例 id 时发到不存在的会话。
  - 定时问候一条都没发出去时，通知文案改为「⚠️ …没有发出去」，不再统一写「已发送」。
- `/空间状态` 与 `/空间问候` 回执新增「发送地址」一行（形如 `睦:FriendMessage:123456`），
  排查发不出去时先看这一行。
- README 常见问题补上「开了定时问候但没收到」的四步排查法与协议端定位方法。
- 自测 305 → **314 项**（新增发送结果校验、失败不记录、平台缺失、手动不占名额、会话地址优先等 9 项）。

## [2.4.0] - 2026-09-22

### 新增

- **互动时间窗口**（`interact_days`，默认 3 天）：每个关注的好友只看窗口内**最新的一条**
  说说；如果这位好友最新一条都超出了窗口，就整体跳过——不点赞也不评论，免得去评论
  几天前的老说说。`interact_count` 现在只负责「拉几条回来挑」，不再逐条处理。
  手动巡检的汇总里会写明「N 人最近一条超出时间窗口」；接口没给时间戳（字段为 0）时
  按「在窗口内」处理，不会因为字段缺失被整批误跳过。
- **`/空间自动发`**：手动触发一次「AI 生成 + 发布」，走的是和定时发布**完全相同**的链路
  （人设、今日日程、联网素材、Token 用量提示都会带上；开着 `draft_enabled` 就先转草稿
  等你确认）。别名 `space auto` / `qz auto` / `空间生成`。
- 自测从 292 项增加到 **305 项**：新增互动时间窗口 8 项（窗口外整体跳过、窗口内多条只取
  最新、天数可调、跳过自己发的、时间戳缺失不误判）与手动自动发 5 项。

## [2.3.1] - 2026-09-22

### 修复

- 自测与检查脚本（`run_tests.py` / `check_metadata.py` / `check_schema.py` / `check_logo.py`）
  在 Windows 默认的 GBK 控制台下，最后一行打印中文与 ✅ 会抛 `UnicodeEncodeError`，
  看起来像「检查失败」其实内容全通过；现在都会先把标准输出切到 UTF-8。
- README 与实际行为对齐：测试数（238 / 234 → 292）、英文别名的写法（别名匹配整条指令名，
  必须带 `space ` 或 `qz ` 前缀，单独写 `/post` 不触发）、补上遗漏的 `/空间登录` 别名、
  补上 `life_pool` 的 4 个子键名，开发章节点名 `tools/make_release_zip.py`。
- 发布包不再夹带运行期残留：`tools/make_release_zip.py` 现在排除 `data/`
  （本地调试产生的文转图模板、`cmd_config.json` 之类）、`*.zip`、`.env` 与覆盖率文件。

## [2.3.0] - 2026-09-22

### 新增

- **回执图渲染**（`core/render.py`）：发布通知、草稿确认、问候与超时提示可以附加一张
  渲染出来的回执图（`notify_render_image`）。用 **AstrBot 自带的文转图**
  （`html_renderer.render_t2i`），不引入字体或图像依赖、包体积不变。
  默认走**本地渲染**（内容不出本机），需要更好观感时可用 `notify_render_network`
  切到 AstrBot 面板配置的 t2i 端点；渲染失败、文本过短都会自动降级为纯文本，
  绝不因此丢掉回执。
- `/空间状态` 新增「回执图渲染」一行，显示开关与渲染方式。

## [2.2.0] - 2026-09-22

### 新增

- **AI 自己确认并引用人格与日程**：生成说说前，提示词会要求 AI 先确认
  「我是谁（AstrBot 的全局人格）、我今天在做什么（当日日程）」，
  再在正文里自然带出今日行程的具体细节；日程缺失时会先生成，
  彻底拿不到时退化为「先按性格为自己安排今天再写」，并在状态里留警告。
  新增 `llm_life_must_reference` 控制是否强制引用细节。
- **Token 用量提示**（`core/usage.py`）：按文本估算每次生成的 token 用量，
  按天与功能累计；`/空间状态` 显示今日用量，新增 `/空间用量 [天数]` 查看明细，
  草稿与发布/问候通知里也会附一行本次用量。
- **更多审核限制选项**：新增 `draft_for_greet`（定时问候也走草稿确认）、
  `draft_timeout_minutes`（草稿超时无人处理则自动放行并通知）；
  重启后遗留草稿会按同一规则继续处理。
- `/空间状态` 新增「上次生成依据」一行（用了哪个人格、是否引用日程、是否用联网素材）。

## [2.1.0] - 2026-09-22

### 新增

- **定时问候**（`core/greet.py`）：按时间给指定用户私聊发早安 / 晚安，
  支持文案池或 AI 按人设与当日日程生成，同一天同一时段对同一个人只发一次；
  新增 `/空间问候` 指令（开关 + 手动测试发送）。
- **插件内管理员名单** `admin_uins`：填了就用它作为草稿确认与通知的接收人，
  留空则回退 AstrBot 的 `admins_id`；新增 `/空间管理员` 指令查看与增删，
  `/空间状态` 会显示识别到的管理员及其来源。
- 问候可单独指定 AstrBot 提供商：`llm_greet_provider_id`。

### 移除

- **直连 API 密钥那条路**：删除 `llm_mode` / `llm_api_base` / `llm_api_key` /
  `llm_model` / `llm_timeout`，以及 `core/llm.py` 里的自建 HTTP 客户端。
  插件现在只使用 AstrBot 面板里配置好的提供商，不再保存任何密钥、不再自己发请求。
- **与其他插件的联动**：删除 `life_source` / `life_external_path`，不再读取
  `astrbot_plugin_life_scheduler` 的数据；生活日程改为完全由本插件生成。

### 修复

- 问候开关在面板里改动后不会自动重启定时任务：现在提供 `/空间问候 on|off`，
  改完立即生效（其余定时任务同理，均支持热更新）。

## [2.0.0] - 2026-09-22

### 新增

- **接入 AstrBot 自带联网搜索**（`core/web.py`）：写说说前可选先联网取资料，
  服务商与密钥复用 AstrBot 面板里的「联网搜索」配置，插件不重复要 key；
  自动适配各家不同的条数参数名，并在失败时降级为普通生成。
  新增 `/空间搜索` 指令用于单独验证接入是否正常（不会发说说）。
  该能力需要 AstrBot ≥4.26（内置搜索工具从该版本开始提供），
  更老版本会自动跳过联网并在状态里提示原因，其余功能不受影响。
- **统一 AI 接入层**：写说说、生成日程、生成评论都通过同一层；既可复用 AstrBot 已配置的
  LLM 提供商，也可直接填写 `api_base` / `api_key` / `model` 直连任意 OpenAI 兼容接口。
- **一体化生活日程**：`life_source=builtin` 时插件自己用 AI 生成当日穿搭与日程
  （按天缓存、懒加载、创意池、历史防重复）；`life_source=external` 时读取
  `astrbot_plugin_life_scheduler` 已生成的数据，不重复消耗 token。
  可选把日程注入 system prompt（默认关闭）。
- **说说互动**：定时读取关注 QQ 号的最近说说（默认只读），可选自动点赞与 AI 评论，
  按 `uin_tid` 去重，同一条说说不会处理两次。
- **草稿确认模式**：自动发布/自动评论先发给管理员私聊与 `draft_umo` 指定会话确认，
  新增 `/空间确认`、`/空间放弃`、`/空间重写` 三个指令；草稿持久化，确认失败会保留内容。
- 新增指令 `/空间日程`、`/空间读说说`、`/空间互动`。
- 按功能分别选择 AI 提供商：`llm_provider_id`（写说说）、`llm_life_provider_id`（日程）、
  `llm_comment_provider_id`（评论）。

### 变更

- 配置项从 20 项扩展到 49 项，`content_source` / `llm_mode` / `life_source` 改为下拉选择。
- 配置默认值改为直接从 `_conf_schema.json` 读取，避免两处默认值不一致。
- 数据目录名改为从 `metadata.yaml` 推导，重命名插件后不会再写错数据目录。

### 修复

- 图片上传返回的 URL 形如 `?bo=` 时也会被正确解析（原实现只认 `&bo=`）。
- 定时发布失败、内容为空、配置非法时都给出可读报错，不再静默失败。

## [1.0.0] - 2026-09-22

### 新增

- 首个版本：复用 OneBot 登录态自动获取 `user.qzone.qq.com` 域 Cookie，免手动抓包。
- 手动发布说说（支持附带图片）、定时自动发布（Cron + 随机抖动）。
- 内容来源：文案池 / 文本文件 / AI 生成，可注入人设与最近聊天记录。
- 发布历史、失败通知、按 tid 删除说说。
